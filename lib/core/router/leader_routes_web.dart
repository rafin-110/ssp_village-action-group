import 'package:flutter/material.dart';

class ReportIssueScreen extends StatelessWidget { const ReportIssueScreen({super.key}); @override Widget build(BuildContext c) => const SizedBox(); }
class IssueListScreen extends StatelessWidget { const IssueListScreen({super.key}); @override Widget build(BuildContext c) => const SizedBox(); }
class IssueDetailScreen extends StatelessWidget { final String issueId; const IssueDetailScreen({super.key, required this.issueId}); @override Widget build(BuildContext c) => const SizedBox(); }
class AddProgressScreen extends StatelessWidget { final String issueId; const AddProgressScreen({super.key, required this.issueId}); @override Widget build(BuildContext c) => const SizedBox(); }
class MeetingListScreen extends StatelessWidget { const MeetingListScreen({super.key}); @override Widget build(BuildContext c) => const SizedBox(); }
class CreateMeetingScreen extends StatelessWidget { const CreateMeetingScreen({super.key}); @override Widget build(BuildContext c) => const SizedBox(); }
class MeetingDetailScreen extends StatelessWidget { final String meetingId; const MeetingDetailScreen({super.key, required this.meetingId}); @override Widget build(BuildContext c) => const SizedBox(); }
class VillageDetailScreen extends StatelessWidget { final String villageId; const VillageDetailScreen({super.key, required this.villageId}); @override Widget build(BuildContext c) => const SizedBox(); }
class VillageListScreen extends StatelessWidget { const VillageListScreen({super.key}); @override Widget build(BuildContext c) => const SizedBox(); }
